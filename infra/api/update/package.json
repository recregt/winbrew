{
	"name": "update",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev:bootstrap": "node ./scripts/dev-bootstrap.mjs",
		"predev": "pnpm run dev:bootstrap",
		"prestart": "pnpm run dev:bootstrap",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev -c ./wrangler.dev.jsonc",
		"start": "wrangler dev -c ./wrangler.dev.jsonc",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.15.0",
		"@types/node": "^25.6.0",
		"typescript": "^6.0.0",
		"vitest": "^4.0.0",
		"wrangler": "^4.87.0"
	},
	"pnpm": {
		"overrides": {
			"undici": "8.1.0"
		}
	}
}